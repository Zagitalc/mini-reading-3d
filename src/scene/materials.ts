import * as T from'three';
export function buildingMaterial(){const canvas=document.createElement('canvas');canvas.width=256;canvas.height=256;const c=canvas.getContext('2d')!;c.fillStyle='#e5d9cc';c.fillRect(0,0,256,256);c.strokeStyle='#b9aea4';c.lineWidth=1;for(let y=0;y<256;y+=12){c.beginPath();c.moveTo(0,y);c.lineTo(256,y);c.stroke();for(let x=(y/12%2)*16;x<256;x+=32){c.beginPath();c.moveTo(x,y);c.lineTo(x,y+12);c.stroke();}}for(let y=24;y<256;y+=85)for(let x=26;x<256;x+=64){c.fillStyle='#e8e4dc';c.fillRect(x-3,y-3,37,48);c.fillStyle='#50656b';c.fillRect(x,y,31,40);c.fillStyle='#7f9295';c.fillRect(x+2,y+2,12,16);c.fillStyle='#d4d4cc';c.fillRect(x+14,y,2,40);c.fillRect(x,y+19,31,2);c.fillStyle='#aaa296';c.fillRect(x-5,y+41,40,4);}const texture=new T.CanvasTexture(canvas);texture.colorSpace=T.SRGBColorSpace;texture.wrapS=texture.wrapT=T.RepeatWrapping;texture.anisotropy=4;return new T.MeshLambertMaterial({map:texture,vertexColors:true,side:T.DoubleSide});}
